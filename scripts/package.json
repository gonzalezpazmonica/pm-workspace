{
  "name": "pm-workspace-scripts",
  "version": "1.0.0",
  "description": "Scripts auxiliares para el PM Workspace — Azure DevOps + Claude Code",
  "main": "report-generator.js",
  "scripts": {
    "install-deps": "npm install",
    "report:hours": "node report-generator.js --type hours",
    "report:executive": "node report-generator.js --type executive --format pptx"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "pptxgenjs": "^3.12.0",
    "docx": "^8.5.0",
    "yargs": "^17.7.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "_install_note": "Ejecutar 'npm install' en la carpeta scripts/ antes de usar report-generator.js"
}
