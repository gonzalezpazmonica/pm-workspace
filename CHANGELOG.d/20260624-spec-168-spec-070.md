# SPEC-168 + SPEC-070

**Date:** 2026-06-24

## SPEC-168 -- Actor Iterative Pre-Action inner loop

- `scripts/actor-pre-action-loop.py`: iterates up to --max-iterations using world-model-simulator
  (SPEC-165). Refines action below threshold. JSON output: approved_action, final_confidence,
  iterations, simulation_history, verdict.
- `tests/scripts/test_actor_pre_action.py`: 26 pytest — approved-at-1, iterate, max-iter,
  history length, verdict values, schema, master-switch-off
- `tests/bats/test-spec-168.bats`: 6 BATS — script syntax, JSON, max-iterations-1, dep-check,
  verdict field, switch=off
- Master switch: SAVIA_ACTOR_PRE_ACTION=on|off (default off)
- pytest 26/26, BATS 6/6

## SPEC-070 -- Security Auto-Remediation PRs

- `scripts/security-remediation-generator.py`: fix suggestions for 10 vuln types
  (sqli, xss, hardcoded-cred, path-traversal, cmd-injection, deserialization, csrf,
  data-exposure, broken-auth, open-redirect); OWASP refs + confidence scores
- `scripts/security-auto-remediation.sh`: finding->fix->branch->draft-PR pipeline;
  infers vuln type from text; writes output/security-fixes/{hash}.md;
  creates agent/security-fix-* branch for severity=high
- `output/security-fixes/`: directory for fix proposals
- `tests/scripts/test_security_remediation.py`: 68 pytest
- `tests/bats/test-spec-070-remediation.bats`: 6 BATS
- Master switch: SAVIA_AUTO_REMEDIATION=on|off (default off)
- Safety: NUNCA modifica codigo, PR siempre Draft, AUTONOMOUS_REVIEWER siempre asignado
- pytest 68/68, BATS 6/6
