# feat(hooks): PAT-shape detection, overnight CI mode, 5 research specs

## Changes

- `block-credential-leak.sh`: PAT-shaped string detection (40+ chars in auth context)
- `block-pat-file-write.sh`: new hook blocks direct PAT writes to versioned files
- `overnight-sprint`: new `--mode ci-unblock` iterates blocked PRs by PR# ASC via nidos.sh
- Research specs SE-223..227 added as PROPOSED (none active)
