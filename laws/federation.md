# Federation Laws

## LAW-FED-001 — Local re-evaluation
A remote Savia instance MUST NOT obtain capabilities that the local instance would deny to a local agent
under the same effective policy (SE-386 §11).
- Verificación: receiver re-evaluates CRITERIO/safety/human-gates localmente (futuro contrato federado).
